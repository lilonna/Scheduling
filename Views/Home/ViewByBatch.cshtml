@model List<Scheduling.Models.Schedule>

@{
    ViewData["Title"] = "Schedules by Batch";
}

<h2>Schedules by Batch</h2>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Batch</th>
            <th>Section</th>
            <th>Course</th>
            <th>Instructor</th>
            <th>Day</th>
            <th>Start - End</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var schedule in Model)
        {
            <tr>
                <td>@schedule.Allocation.Section.BatchId</td>
                <td>@schedule.Allocation.Section.Id</td>
                <td>@schedule.Allocation.Course.Name</td>
                <td>@schedule.Allocation.Instructor.FullName</td>
                <td>@schedule.TimeSlot.DaysOfWeek.Name</td>
                <td>@schedule.TimeSlot.From- @schedule.TimeSlot.To</td>
            </tr>
        }
    </tbody>
</table>
